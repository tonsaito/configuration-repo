server:
    port: 5200
    error:
        include-stacktrace: never

spring:
    application:
      name: payment-api
    profiles:
      active: ${PROFILES_ACTIVE:dev}
    cloud:
        consul:
        host: localhost
        port: 8500
        failFast: false